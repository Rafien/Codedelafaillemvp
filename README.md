
  # Code de la Faille MVP

  This is a code bundle for Code de la Faille MVP. The original project is available at https://www.figma.com/design/QM8ZclfUJRe7ecxmWdO0r6/Code-de-la-Faille-MVP.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  